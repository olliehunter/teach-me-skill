# Desktop Extensions: One-click MCP server installation (Anthropic Engineering)

URL: https://www.anthropic.com/engineering/desktop-extensions
Tier: 1 (primary — Anthropic engineering blog, original source of the format)
Published: Jun 26, 2025. Accessed: 2026-07-01

## Rename note (Sep 11, 2025)
"Claude Desktop Extensions now use the .mcpb (MCP Bundle) file extension instead of .dxt. Existing
.dxt extensions will continue to work, but we recommend developers use .mcpb for new extensions going
forward. All functionality remains the same - this is purely a naming convention update."

## The installation problem MCPB solves
Local MCP servers were powerful but hard to install: users needed developer tools (Node.js, Python),
had to manually edit JSON config files, resolve dependency conflicts, find servers on GitHub, and
manually reinstall to update. "These friction points meant that MCP servers, despite their power,
remained largely inaccessible to non-technical users."

Before: install Node.js, `npm install -g @example/mcp-server`, edit
`~/.claude/claude_desktop_config.json`, restart. After: (1) download a `.mcpb` file, (2) double-click
to open with Claude Desktop, (3) click "Install." "That's it. No terminal, no configuration files, no
dependency conflicts."

## Architecture
"A Desktop Extension is a zip archive containing the local MCP server as well as a manifest.json."
```
extension.mcpb (ZIP archive)
├── manifest.json     # Required: metadata and configuration
├── server/           # MCP server implementation
├── node_modules/     # (Node) bundled dependencies   / lib/ for Python
└── icon.png          # Optional icon
```
"The only required file in a Desktop Extension is a manifest.json."

Claude Desktop handles the complexity:
- **Built-in runtime**: "We ship Node.js with Claude Desktop, eliminating external dependencies."
- **Automatic updates**: extensions update automatically when new versions are available.
- **Secure secrets**: "Sensitive configuration like API keys are stored in the OS keychain."

Three supported extension types: Node.js, Python, and classic binaries/executables.

## The manifest and user config
"the developer declares that the user needs to supply an api_key. Claude will not enable the extension
until the user has supplied that value, keep it automatically in the operating system's secret vault,
and transparently replace the ${user_config.api_key} with the user-supplied value when launching the
server. Similarly, ${__dirname} will be replaced with the full path to the extension's unpacked
directory."

## Building your first extension (the CLI)
- Step 1 — Create the manifest: `npx @anthropic-ai/mcpb init` (interactive; `--yes` for the most basic
  manifest).
- Step 2 — Declare user configuration (e.g. `allowed_directories`).
- Step 3 — Package: `npx @anthropic-ai/mcpb pack` — "Validates your manifest" and "Generates the .mcpb
  archive."
- Step 4 — Test locally: "Drag your .mcpb file into Claude Desktop's Settings window."

Get started:
```
npm install -g @anthropic-ai/mcpb
mcpb init
mcpb pack
```

## Open ecosystem
"we're open-sourcing the Desktop Extension specification, toolchain, and the schemas and key functions
used by Claude for macOS and Windows." "For MCP server developers: Package once, run anywhere that
supports MCPB." The spec/toolchain was versioned 0.1 at launch to invite community evolution.

## Enterprise controls
Sensitive data in the OS keychain; audit installed extensions; Group Policy (Windows) and MDM (macOS)
support; pre-install approved extensions; blocklist specific extensions/publishers; disable the
extension directory; deploy private extension directories.

## Building extensions with Claude Code
Anthropic recommends telling Claude Code to read the MCPB README, MANIFEST.md, and examples, then
implement an MCP server using @modelcontextprotocol/sdk with stdio transport, proper tool schemas,
error handling, and timeout management.
